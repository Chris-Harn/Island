#include "SDL/SDL.h"
#include "SDL/SDL_ttf.h"

int main( int argc, char **argv ) {
	SDL_Surface *window;
	SDL_Event event;
	bool RUNNING = true;

	while( RUNNING ) {

		RUNNING = false;
	}

	return 0;
}
